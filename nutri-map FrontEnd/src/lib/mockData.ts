// Temporary no-op mock data module
// Previously the project used a mock seeder during early development.
// The real backend is now used. This module is a safe no-op to satisfy any
// lingering imports during hot-reload while we finish removing mocks.

export function seedMockData() {
  // no-op
}
